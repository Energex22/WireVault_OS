WireVault Alpha 1.5.1 — Profile Editor Visibility Fix

Required baseline: Alpha 1.5

Copy all patch files into the repository root, replace files, restart Core,
open a fresh browser tab, open the profile panel, and click Edit beside the
active profile.

Commit: Make profile editor controls visible
