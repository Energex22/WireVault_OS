WireVault Alpha 0.9.6.2 — Home Cache Bust Hotfix

Required baseline: Alpha 0.9.6.1

1. Copy all patch files into your WireVault_OS repository root.
2. Replace the existing files.
3. Close every open WireVault browser tab.
4. Close WireVault Core.
5. Start WireVault Core again.
6. Open a new browser tab to WireVault.

This update changes the JavaScript and CSS URLs to versioned URLs so Chromium cannot reuse the old Home module.

The corrected Home page contains one large Raspberry Pi System card and does not contain separate CPU Temperature, Memory, or Storage cards.

Commit message:
Force reload of corrected Home system card
